<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YallaMeal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/color2</item>
        <item name="colorSecondary">@color/color1</item>

    </style>

    <style name="Theme.YallaMeal" parent="Base.Theme.YallaMeal" />
<!--    <style name="Base.Theme.YallaMeal" parent="Theme.Material3.DayNight.NoActionBar"/>-->
    <!-- res/values/styles.xml -->
    <style name="FilterButton">
        <item name="android:padding">12dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/selector_filter_unselected</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="FilterButton.Selected" parent="FilterButton">
        <item name="android:background">@drawable/selector_filter_selected</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="FilterButton.Unselected" parent="FilterButton"/>
</resources>